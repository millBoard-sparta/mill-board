package com.sparta.millboard.repository;

import com.sparta.millboard.model.BoardPartner;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardPartnerRepository extends JpaRepository<BoardPartner, Long> {
}
